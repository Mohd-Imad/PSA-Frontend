//setInterval()----is a javscript builtin function which pass arguments as arrow function and time in milli seconds and executes the function  every given time

// setInterval(1,2)

setInterval(() => {
    console.log('Testing setInterval method');
}, (2000));                                      //here 2000ms == 2secs

//here, the function ll execute every 2 secs