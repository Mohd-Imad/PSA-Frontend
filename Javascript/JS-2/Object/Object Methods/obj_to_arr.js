let person={}                                    //creating an empty object
if(Object.keys(person).length > 0){              //Objet.keys---used to convert from object to array //after conversion length property is used to check the size of array
    console.log("Object is not empty");
}
else{
    console.log("Object is empty");
}